import React from 'react';
import { shallow } from 'enzyme';
import {{name}} from './{{name}}';

describe('Given a {{name}} component', () => {
  it('should render properly', () => {
    let wrapper = shallow(<{{name}} />);

    expect(wrapper).toBeTruthy();
  });

});
